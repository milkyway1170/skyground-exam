import { Box, styled } from '@mui/material'

export const Container = styled(Box)({
  display: 'flex',
  width: '100%',
  height: '100%',
  margin: '2rem auto',
  justifyContent: 'center',
  alignItems: 'center'
})
