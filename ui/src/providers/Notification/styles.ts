import { Box, styled } from '@mui/material'

export const Сontainer = styled(Box)({
  position: 'fixed',
  display: 'flex',
  flexDirection: 'column',
  zIndex: '10000',
  gap: '2rem',
  top: '1rem',
  right: '1rem',
  width: '50rem'
})
