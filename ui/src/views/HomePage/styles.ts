import { Box, styled, Typography } from "@mui/material";

export const Сontainer = styled(Box)({
  width: "100%",
  height: "100%",
});

export const Title = styled(Typography)({
  textAlign: "left",
  fontSize: "3rem",
});

export const GreatingContainer = styled(Box)({
  lineHeight: "100%",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});
